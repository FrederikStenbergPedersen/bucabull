# testdata

Empty for now — see the "What's not covered yet" section of `../README.md`
for why no real `.dem` fixture is vendored here yet. When one is added,
`internal/parse` should get a real `Parse()`-against-a-fixture test
alongside the existing pure-function unit tests in `mapping_test.go`.
