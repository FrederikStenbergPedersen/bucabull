// CT=info(cyan)/T=warning(orange) — the established side-color convention,
// first introduced by Utility's lineup cards and side filter, now shared by
// Strategies too so the two features never drift apart.
export const SIDE_DOT: Record<string, string> = {
    CT: 'bg-accent-secondary',
    T: 'bg-warning',
};
